{
    "elasticsearch": {
        "cluster_name": "elasticsearch",
        "client_transport_sniff": false,

        "transportAddresses": [
            {
                "hostname": "${test.es.hostname}",
                "port": ${test.es.port}
            }
        ]
    }
}